FROM node:22-alpine

RUN npm install -g tsx@latest && npm cache clean --force

RUN addgroup -S sandbox && adduser -S sandbox -G sandbox

WORKDIR /sandbox
RUN chown sandbox:sandbox /sandbox

USER sandbox

ENTRYPOINT ["sh", "-c"]
